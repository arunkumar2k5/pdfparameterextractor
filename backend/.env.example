# OpenAI API Configuration
OPENAI_API_KEY=your-api-key-here

# OpenAI Model Selection
# Options:
#   - gpt-3.5-turbo (faster, cheaper, good for simple extractions)
#   - gpt-4 (more accurate, better reasoning, higher cost)
#   - gpt-4-turbo (faster GPT-4, good balance)
#   - gpt-4o (latest optimized model)
OPENAI_MODEL=gpt-3.5-turbo

# Development Mode (uses cached PDF/markdown to skip slow conversion)
DEV_MODE=True
